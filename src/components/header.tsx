import React, { useState } from "react";

export default function Header() {
  return (
    <header id="header">
      <h1>Siege Pillage Stone Reward Breakdown</h1>
      <nav>
        <p>
          This site is intended to allow you to enter the collected taxes on
          your server and breakdown the amount each pillage point will
          hold/reward at each interval.
        </p>
      </nav>
    </header>
  );
}
